# 🌟 Sihatod: Content-Addressable Storage с mTLS Protected Layer

## 🛡️ Безопасность уровня Enterprise

В текущей версии реализован **Уровень 1: Транспортная безопасность**. Доступ к хранилищу защищен двусторонним TLS (mTLS). Это гарантирует, что:

1. **Приложение доверяет только вашему Redis:** Проверка через Root CA с валидацией цепочки доверия (SKI/AKI).
2. **Redis доверяет только вашему приложению:** Использование клиентских сертификатов с расширением `CLIENT_AUTH`.
3. **Защита от MITM:** Полная проверка имен хостов (hostname) и цепочек сертификатов в Python 3.14+.

## 🚀 Возможности

* **Content-Addressable Storage (CAS):** Детерминированная адресация через SHA256-хеш.
* **Идемпотентность и дедупликация:** Идентичные данные не дублируются и всегда имеют один адрес.
* **mTLS Шифрование:** Весь трафик между FastAPI и Redis зашифрован и аутентифицирован.

## 📦 Быстрый запуск (с учетом безопасности)

### 1. Подготовка сертификатов

Перед запуском контейнеров необходимо создать инфраструктуру закрытых ключей (PKI). Скрипт `generate_certs.py` создаст корневой сертификат (CA) и универсальный сертификат для mTLS.

```bash
# Установите зависимости (cryptography)
pip install cryptography

# Сгенерируйте сертификаты
python generate_certs.py

```

*Это создаст папку `certs/` с файлами `ca.crt`, `redis.crt` и `redis.key*`.

### 2. Настройка окружения

Создайте файл `.env` и укажите в нем безопасный URL для подключения к Redis (используйте протокол `rediss://` для TLS):

```env
REDIS_URL=rediss://default:your_secure_password@redis:6379/0

```

### 3. Сборка и запуск

```bash
docker-compose up --build -d

```

*Docker автоматически пробросит сгенерированные сертификаты в контейнеры для взаимной аутентификации.*

## 🧪 Технологический Стек

* **Python 3.14+** (строгая проверка SSL/TLS).
* **FastAPI & Redis** (асинхронная обработка данных).
* **Cryptography** (генерация эталонных X.509 сертификатов).

## 🛠️ Примеры использования (PowerShell)

**Создание записи (автоматическая генерация хеш-адреса):**

```powershell
Invoke-RestMethod -Method Put -Uri "http://localhost:5001/hash/user_1/account_type?value=checking"

```

**Чтение по адресу:**

```powershell
Invoke-RestMethod -Method Get -Uri "http://localhost:5001/hash/user_1/account_type"

```

---

## 📄 Лицензия

Этот проект распространяется под лицензией MIT.