# 🌟 Sihatod: The Zero-Trust Storage Sovereign (v1.2.0)

**Sihatod** (Secure Intelligent Hash-Addressable Transparent Object Database) — это не просто хранилище, это высокозащищенное асинхронное хранилище данных, построенное на принципах **Zero Trust Architecture**. Проект является технологическим эталоном безопасности начала 2026 года, где каждый байт данных защищен на четырех независимых уровнях. Проект создан как эталон того, как должна выглядеть защита данных в мире, где «периметра больше нет». Проект использует **асимметричное шифрование**.

## 🛡️ Философия безопасности (AAA+ Tier)

Мы внедрили эшелонированную защиту, где каждый слой не доверяет предыдущему:
### Уровень 1: Транспорт (mTLS 1.3)

* **Mutual TLS:** Полная двусторонняя аутентификация между FastAPI и Redis с использованием X.509 сертификатов.
* **Integrity:** Проверка цепочек доверия через Root CA с валидацией SKI/AKI идентификаторов.
* **Redis ACL:** Доступ ограничен пользователем с правами только на чтение/запись в префиксе `sihatod:*`.

### Уровень 2: Инфраструктура (Hardened Container)

* **Rootless Docker:** Контейнер запускается от непривилегированного пользователя `sihatoduser` (UID 10001).
* **Minimal Attack Surface:** Запрещен системный шелл, итеративная сборка минимизирует количество установленных библиотек.

### Уровень 3: Идентификация (EdDSA & Secure Cookies)

* **Ed25519:** Использование современных эллиптических кривых для подписи JWT токенов.
* **Anti-XSS:** Токены передаются исключительно в `HttpOnly`, `Secure` и `SameSite=Strict` Cookies.

### Уровень 4: Приложение (ALE & Zero-PII)

* **Application Level Encryption (ALE):** Данные шифруются AES-256-GCM внутри Python перед отправкой в хранилище. Redis видит только зашифрованный шум.
* **Zero-PII Logs:** Переход на передачу данных в Body (JSON) гарантирует отсутствие персональных данных в логах сервера (URL query logs).

## 🚀 Технологический стек

* **Runtime:** Python 3.14+ (Strict SSL/TLS Enforcement).
* **Framework:** FastAPI (Async Integrity).
* **Storage:** Redis 7.4+ (ACL Restricted & TLS).
* **Auth:** Ed25519 (Asymmetric JWT Signing).
* **Crypto:** AES-256-GCM (Fernet implementation).

## **📦 Быстрый запуск**

### 1. Подготовка криптографической среды

Перед запуском необходимо сгенерировать два типа ключей: для сетевой защиты (mTLS) и для аутентификации пользователей (JWT).

```bash
# Установите зависимости
pip install cryptography

# А) Генерация сертификатов mTLS (Транспортный слой)
python generate_certs.py

# Б) Генерация Ed25519 ключей (Слой идентификации)
python generate_auth_keys.py

```

*В папке `certs/` появятся `ca.crt`, `redis.crt`, `redis.key` для защиты трафика, а также `jwt_private.pem` и `jwt_public.pem` для безопасной работы с токенами. Без них система аутентификации просто не запустится, так как `AuthService` не сможет подписывать и проверять токены.*

### 2. Конфигурация безопасности

Настройте `.env` (убедитесь, что `MASTER_ENCRYPTION_KEY` сгенерирован и надежно спрятан):

```env
REDIS_URL=rediss://sihatod_app:password@redis:6379/0
MASTER_ENCRYPTION_KEY=your_32_byte_base64_key
DEBUG=false

```

### 3. Запуск крепости

```bash
docker-compose up --build -d

```

## 🛠️ API Interaction (The Clean Way)

Благодаря переходу на передачу данных в Body, ваши запросы теперь не оставляют следов в системных журналах:

**Безопасная запись (PowerShell):**

```powershell
$body = @{ value = "TopSecret100500" } | ConvertTo-Json
Invoke-RestMethod -Method Put -Uri "https://localhost:5001/hash/user_1/balance" -Body $body -ContentType "application/json"

```

**Безопасное чтение:**

```powershell
Invoke-RestMethod -Method Get -Uri "https://localhost:5001/hash/user_1/balance"

```

**Пакетное чтение:**

```powershell
$hashes = @("hash_1", "hash_2")
Invoke-RestMethod -Method Post -Uri "https://localhost:5001/hash/batch" -Body ($hashes | ConvertTo-Json) -ContentType "application/json"

```

## 📉 Мониторинг и лимиты

Приложение оснащено встроенным **FastAPILimiter**. При попытке перебора или DDoS, система автоматически блокирует IP на уровне Redis, предотвращая деградацию сервиса.

---

### 🏛️ Статус проекта

Этот проект является **Reference Architecture**. Он демонстрирует максимально возможный уровень защиты для веб-приложений на текущий момент времени.

**Лицензия:** MIT — с уважением к безопасности и открытому коду.
Статус:**Completed**

*Внимание, в реальном продакшн использовании потребуются доработки/переработки в части бизнес-логики, генерации сертификатов и можество дополнительных индивидуальных требований организации.*

---

*Sihatod: Храним так, будто за нами наблюдает весь мир.*

---