# 🌟 Sihatod: Кварковое Хранилище Данных (Content-Addressable Key-Value Store)

![Quantum Storage](https://image.pollinations.ai/prompt/abstract-glowing-data-storage-network-nodes-and-connections-in-a-dark-sci-fi-style)

## Описание

**Sihatod** — это ультра-лаконичное, высокопроизводительное и масштабируемое Key-Value хранилище данных, разработанное по принципу **Content-Addressable Storage (CAS)**. Вместо традиционной адресации по ID, Sihatod использует детерминированный SHA256-хеш, генерируемый из бизнес-атрибутов (`client_key` + `attribute`), как универсальный ключ. Это обеспечивает:

* **Идемпотентность:** Любое количество операций PUT с одинаковым хешем приводит к одному и тому же состоянию.
* **Дедупликация:** Идентичные данные всегда хранятся по одному и тому же адресу, экономя пространство.
* **Скорость:** Доступ к данным происходит напрямую по хешу, гарантируя O(1) сложность (почти мгновенно) независимо от объема хранимых данных.
* **Универсальность:** Система абстрагируется от бизнес-логики и типа данных, становясь универсальным слоем для хранения любых состояний.

Проект состоит из легковесного FastAPI-сервиса на Python и использует Redis как высокопроизводительное и устойчивое к отказам NoSQL-хранилище.

## 🚀 Возможности

* **Гибридная адресация:**
    * Доступ к данным по сгенерированному хешу (`client_key` + `attr`).
    * Прямой доступ по предварительно известному хеш-адресу (`addr`).
* **Полный CRUD:** Поддержка операций `GET`, `PUT`, `DELETE`.
* **Высокая производительность:** FastAPI (asyncio) + Uvicorn + Redis обеспечивают обработку тысяч запросов в секунду.
* **Легковесность:** Минимум зависимостей, чистый Python-код, быстрый запуск.
* **Масштабируемость:** Архитектура Stateless-сервиса позволяет легко горизонтально масштабировать приложение.
* **Устойчивость:** Идемпотентность операций гарантирует целостность данных даже при сетевых сбоях и повторных запросах.

## 🛠️ Технологический Стек

* **FastAPI:** Быстрый Python-фреймворк для создания API.
* **Uvicorn:** ASGI-сервер для запуска FastAPI.
* **Redis:** Высокопроизводительное Key-Value хранилище, используемое в качестве основной БД.
* **Poetry:** Управление зависимостями Python.
* **Docker & Docker Compose:** Контейнеризация и оркестрация сервисов.

## 📦 Быстрый Запуск

### 1. Клонирование репозитория

```bash
git clone <URL_ВАШЕГО_РЕПОЗИТОРИЯ>
cd Sihatod

```

### 2. Подготовка файлов

Убедитесь, что у вас есть следующие файлы:

* `main.py` (файл с кодом FastAPI)
* `pyproject.toml` (с зависимостями, указанными выше)
* `Dockerfile` (для сборки образа приложения)
* `docker-compose.yml` (для запуска приложения и Redis)

### 3. Сборка и запуск с Docker Compose

```bash
docker-compose up --build -d

```

Это соберет образ `Sihatod` и запустит два сервиса: `app` (FastAPI) на порту `5001` и `redis` на порту `6379`.

## 🧪 Примеры использования (PowerShell)

### 1. Создание/Обновление данных (PUT)

**По `client_key` и `attr`:**

```powershell
Invoke-RestMethod -Method Put -Uri "http://localhost:5001/hash/user123/balance?value=5000"

```

**По прямому `addr`:**

```powershell
# Предположим, '45' - это некий заранее известный хеш
Invoke-RestMethod -Method Put -Uri "http://localhost:5001/hash/any/any?addr=45&value=9999"

```

### 2. Получение данных (GET)

**По `client_key` и `attr`:**

```powershell
Invoke-RestMethod -Method Get -Uri "http://localhost:5001/hash/user123/balance"

```

**По прямому `addr`:**

```powershell
Invoke-RestMethod -Method Get -Uri "http://localhost:5001/hash/any/any?addr=45"

```

### 3. Удаление данных (DELETE)

**По `client_key` и `attr`:**

```powershell
Invoke-RestMethod -Method Delete -Uri "http://localhost:5001/hash/user123/balance"

```

**По прямому `addr`:**

```powershell
Invoke-RestMethod -Method Delete -Uri "http://localhost:5001/hash/any/any?addr=45"

```

## 🧪 Примеры использования (Python)

``` bash
python do_business_logic.py

```
---

## 🛑 Остановка сервисов

```bash
docker-compose down

```

## 🤝 Вклад

Если у вас есть идеи по улучшению или вы нашли баг, не стесняйтесь открывать Issues или Pull Requests. Мы верим в силу сообщества и открытого исходного кода.

---

## 📄 Лицензия

Этот проект распространяется под лицензией MIT.

---