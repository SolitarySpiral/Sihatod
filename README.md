# 🌟 Sihatod: The Zero-Trust Storage Sovereign (v1.2.0)

**Sihatod** (Secure Intelligent Hash-Addressable Transparent Object Database) — это не просто хранилище, это высокозащищенное асинхронное хранилище данных, построенное на принципах **Zero Trust Architecture**. Проект является технологическим эталоном безопасности начала 2026 года, где каждый байт данных защищен на четырех независимых уровнях. Проект создан как эталон того, как должна выглядеть защита данных в мире, где «периметра больше нет». Проект использует **асимметричное шифрование**.

## 🛡️ Философия безопасности (AAA+ Tier)

Мы внедрили эшелонированную защиту, где каждый слой не доверяет предыдущему:
### Уровень 1: Транспорт (mTLS 1.3)

* **Mutual TLS:** Полная двусторонняя аутентификация между FastAPI и Redis с использованием X.509 сертификатов.
* **Integrity:** Проверка цепочек доверия через Root CA с валидацией SKI/AKI идентификаторов.
* **Redis ACL:** Доступ ограничен пользователем с правами только на чтение/запись в префиксе `sihatod*:*`.

### Уровень 2: Инфраструктура (Hardened Container)

* **Rootless Docker:** Контейнер запускается от непривилегированного пользователя `sihatoduser` (UID 10001).
* **Minimal Attack Surface:** Запрещен системный шелл, итеративная сборка минимизирует количество установленных библиотек.

### Уровень 3: Идентификация (EdDSA & Secure Cookies)

* **Ed25519:** Использование современных эллиптических кривых для подписи JWT токенов.
* **Anti-XSS:** Токены передаются исключительно в `HttpOnly`, `Secure` и `SameSite=Strict` Cookies.

### Уровень 4: Приложение (ALE & Zero-PII)

* **Application Level Encryption (ALE):** Данные шифруются AES-256-GCM внутри Python перед отправкой в хранилище. Redis видит только зашифрованный шум.
* **Zero-PII Logs:** Переход на передачу данных в Body (JSON) гарантирует отсутствие персональных данных в логах сервера (URL query logs).

### Уровень 5: 🛡️ Технологический стек безопасности (S-Tier)
* **Encryption at Rest:** AES-256-GCM с деривацией ключей через HKDF.
* **Docker Secrets:** Ключи монтируются в tmpfs (RAM) и не сохраняются на диске контейнера.
* **Hardened Image:** Update rootless docker: Запуск от non-root пользователя (UID 10001) и Read-Only файловая система.

## Что отделяет AAA+ от S-Tier
* **Авторизация клиента**: В текущем решении выключена настоящая авторизация клиента. Вшита в эндпойнт как `admin` и `secret`.
* **Vault-интеграция (Secret Management)**: Сейчас MASTER_ENCRYPTION_KEY лежит в секрете докера. В S-Tier приложении код вообще не знает ключей. Он запрашивает их у внешнего защищенного хранилища (например, HashiCorp Vault) при старте, и ключи живут только в RAM. (Частично реализовано)
* **Audit Logging (Immutable Trail)**: Добавление отдельного потока логов, которые невозможно удалить или подделать. Каждый доступ к данным (кто, когда, к какому хешу) подписывается тем же Ed25519 ключом и отправляется в хранилище логов. Это делает систему прозрачной для расследований.
* **Circuit Breaker & Fallback**: Если Redis перегружен или mTLS рукопожатие тормозит, S-Tier приложение не «падает», а корректно переходит в режим деградации (например, отдает данные из локального L2-кэша), защищая основную базу от окончательного падения.
* **Post-Quantum Cryptography (PQC)**: В 2026 году это становится стандартом. Замена RSA-сертификатов на квантово-устойчивые алгоритмы (например, Crystals-Kyber). Это защита «на вырост» — чтобы данные, перехваченные сегодня, не расшифровали через 5 лет на квантовом компьютере.

## 🚀 Технологический стек

* **Runtime:** Python 3.14+ (Strict SSL/TLS Enforcement).
* **Framework:** FastAPI (Async Integrity).
* **Storage:** Redis 7.4+ (ACL Restricted & TLS).
* **Auth:** Ed25519 (Asymmetric JWT Signing).
* **Crypto:** AES-256-GCM (Fernet implementation).

## **📦 Быстрый запуск**

### 1. Подготовка криптографической среды

Перед запуском необходимо сгенерировать два типа ключей: для сетевой защиты (mTLS) и для аутентификации пользователей (JWT).

```bash
# Установите зависимости
pip install cryptography
# 1. Генерация SSL инфраструктуры и JWT ключей
python generate_certs.py
python generate_auth_keys.py

# 2. Создание МАСТЕР-КЛЮЧА (Обязательно!)
# Ключ используется для AES-шифрования данных в Redis. 
# Команда создаст файл master_key.key с 32 байтами случайных данных.
python -c "import os; print(os.urandom(32).hex())" > master_key.key
```

*В папке `certs/` появятся `ca.crt`, `redis.crt`, `redis.key` для защиты трафика, а также `jwt_private.pem` и `jwt_public.pem` для безопасной работы с токенами. Без них система аутентификации просто не запустится, так как `AuthService` не сможет подписывать и проверять токены.*

Скрипт `generate_pfx.py` не используется. Служит для объединения сертификатов и ключа в 1 файл.

### 2. Конфигурация безопасности

Настройте `.env` (убедитесь, что `MASTER_ENCRYPTION_KEY` сгенерирован и надежно спрятан):

```env
REDIS_URL=rediss://sihatod_app:password@redis:6379/0
MASTER_ENCRYPTION_KEY=your_32_byte_base64_key
DEBUG=false

```

### 3. Запуск крепости

```bash
docker-compose up --build -d

```

## 🛠️ API Interaction (The Clean Way)

Благодаря переходу на передачу данных в Body, ваши запросы теперь не оставляют следов в системных журналах:

**Простая проверка**
```bash
python .\do_business_logic.py

```

**Безопасная запись:**

```bash
python client_request.py PUT /hash/user_1/balance "{\`"value\`":\`"123\`"}"
--- Результат PUT /hash/user_1/balance ---
Status: 200
{
  "hash": "8767553f87ab5dd590ba5907bbd21d1b64b8354776b225a868be07ca3b2e3111",
  "status": "success"
}

```

**Безопасное чтение:**

```bash
python client_request.py GET /hash/user_1/balance
--- Результат GET /hash/user_1/balance ---
Status: 200
{
  "value": "100500",
  "hash": "8767553f87ab5dd590ba5907bbd21d1b64b8354776b225a868be07ca3b2e3111"
}
```

**Пакетное чтение:**

```bash
python .\client_request.py POST /hash/batch "{\`"hashes\`": [\`"bc4283cd533f31ba016223975ce6f9133a87f428ab3f64e8beb6cfe5573c375d\`", \`"edccb5268c206790663d378e29cdb951516591ddf0fc8a033d8be0cefd8512ff\`"]}"
--- Результат POST /hash/batch ---
Status: 200
{
  "data": {
    "bc4283cd533f31ba016223975ce6f9133a87f428ab3f64e8beb6cfe5573c375d": "checking",
    "edccb5268c206790663d378e29cdb951516591ddf0fc8a033d8be0cefd8512ff": "RUB"
  }
}
```
**Пакетное чтение:**

```bash
python client_request.py DELETE /hash/user_1/balance
--- Результат DELETE /hash/user_1/balance ---
Status: 200
{
  "hash": "8767553f87ab5dd590ba5907bbd21d1b64b8354776b225a868be07ca3b2e3111",
  "deleted": true
}
```

## 🏗️ Механика защиты данных
* Transport: Запрос отклоняется на уровне TLS, если у клиента нет сертификата, подписанного нашим Root CA.
* App Layer: AuthService проверяет EdDSA-подпись JWT токена.
* Crypto: DataProtector извлекает master_key из защищенного хранилища Docker Secrets и выполняет деривацию уникального ключа для каждой записи.
* Storage: В Redis данные уходят по зашифрованному каналу и хранятся в виде нечитаемых объектов (AES-GCM payload + salt + nonce).
*⚠️ Security Warning: Файл master_key.key является критическим элементом. Его потеря приведет к невозможности расшифровать базу данных. Убедитесь, что он добавлен в .gitignore.*

## 📉 Мониторинг и лимиты

Приложение оснащено встроенным **FastAPILimiter**. При попытке перебора или DDoS, система автоматически блокирует IP на уровне Redis, предотвращая деградацию сервиса.

---

### 🏛️ Статус проекта

Этот проект является **Reference Architecture**. Он демонстрирует максимально возможный уровень защиты для веб-приложений на текущий момент времени.

**Лицензия:** MIT — с уважением к безопасности и открытому коду.
Статус:**Completed**

*Внимание, в реальном продакшн использовании потребуются доработки/переработки в части бизнес-логики, генерации сертификатов и можество дополнительных индивидуальных требований организации.*

---

*Sihatod: Храним так, будто за нами наблюдает весь мир.*

---